{% extends 'base.html.twig' %}

{% block content %}
    <h1>Formulaire d'ajout d'un article</h1>
    {{ form_start(form) }}
        {{ form_row(form) }}
        <button type="submit" class="btn btn-primary">
            Valider
        </button>
    {{ form_end(form) }}
{% endblock %}